/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins: ['shanty-plastic-spelling.ngrok-free.dev'],
};

export default nextConfig;
